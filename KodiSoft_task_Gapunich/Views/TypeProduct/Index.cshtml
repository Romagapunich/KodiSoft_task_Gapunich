@model IEnumerable<KodiSoft_task_Gapunich.Typeproducts>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "CreateTypeProduct")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.ActionLink("Edit", "EditTypeProduct", new {id = item.Id}) |
                @Html.ActionLink("Details", "DetaliTypeProduct", new {id = item.Id}) |
                @Html.ActionLink("Delete", "DeleteTypeProduct", new {id = item.Id})
            </td>
        </tr>
    }

</table>
@Html.ActionLink("Back to List", "Index", "Admin")
