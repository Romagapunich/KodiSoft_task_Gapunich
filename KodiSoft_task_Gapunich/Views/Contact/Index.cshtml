
@model IEnumerable<KodiSoft_task_Gapunich.Contacts>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "CreateContact")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Svyazy)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Svyazy)
        </td>
        <td>
            @Html.ActionLink("Edit", "EditContact", new { id=item.Id }) |
            @Html.ActionLink("Details", "DetaliContact", new { id=item.Id }) |
            @Html.ActionLink("Delete", "DeleteContact", new { id=item.Id })
        </td>
    </tr>
}

</table>
@Html.ActionLink("Back to List", "Index", "Admin")